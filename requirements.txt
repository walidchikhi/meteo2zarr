# Core dependencies for NWP to Zarr conversion
numpy
xarray
zarr
dask[complete]
numcodecs
netCDF4
pyyaml

# Meteorological data formats
epygram
cfgrib
eccodes

# Optional visualization / geospatial
rioxarray
ndpyramid
