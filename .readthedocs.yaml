version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  apt_packages:
    - libeccodes-dev
    - libgeos-dev
    - libproj-dev
  jobs:
    post_install:
      - pip install -r docs/requirements.txt
      - pip install -e .

sphinx:
  configuration: docs/source/conf.py
